{
  "name": "maptail",
  "version": "1.0.1",
  "description": "tail geoip data on a world map in realtime",
  "main": "dist/maptail.js",
  "bin": {
    "maptail": "dist/cli.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start": "bun run generate-fake-logs.ts --rate=200/m | bun run src/cli.ts -- --silent",
    "dev": "bun run generate-fake-logs.ts --rate=200/m | bun run --watch src/cli.ts -- --silent"
  },
  "keywords": [
    "geoip",
    "maptail"
  ],
  "author": "stagas",
  "license": "MIT",
  "type": "module",
  "files": [
    "dist",
    "public"
  ],
  "dependencies": {
    "cac": "^6.7.14",
    "db5lite": "^1.0.0",
    "hook-std": "^4.0.0",
    "ip2location-nodejs": "^9.7.0",
    "pkg-dir": "^9.0.0",
    "unzipper": "^0.12.3"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "@types/unzipper": "^0.10.11",
    "typescript": "^5.9.3"
  }
}
